// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: vtuber_image/v1/image.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "vtuber_image.v1";

export interface GenerateRequest {
  /** UUID or stable ID for the persona. */
  personaId: string;
  /** Visual overrides for this specific generation. */
  overrides: PersonaOverrides | undefined;
}

export interface PersonaOverrides {
  hairStyle: string;
  eyeColor: string;
  outfit: string;
}

export interface GenerateResponse {
  /** URL or path to the generated image. */
  imageUrl: string;
  /** Key-value pairs for provenance and workflow metadata. */
  metadata: { [key: string]: string };
}

export interface GenerateResponse_MetadataEntry {
  key: string;
  value: string;
}

function createBaseGenerateRequest(): GenerateRequest {
  return { personaId: "", overrides: undefined };
}

export const GenerateRequest: MessageFns<GenerateRequest> = {
  encode(message: GenerateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.personaId !== "") {
      writer.uint32(10).string(message.personaId);
    }
    if (message.overrides !== undefined) {
      PersonaOverrides.encode(message.overrides, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.personaId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.overrides = PersonaOverrides.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateRequest {
    return {
      personaId: isSet(object.personaId)
        ? globalThis.String(object.personaId)
        : isSet(object.persona_id)
        ? globalThis.String(object.persona_id)
        : "",
      overrides: isSet(object.overrides) ? PersonaOverrides.fromJSON(object.overrides) : undefined,
    };
  },

  toJSON(message: GenerateRequest): unknown {
    const obj: any = {};
    if (message.personaId !== "") {
      obj.personaId = message.personaId;
    }
    if (message.overrides !== undefined) {
      obj.overrides = PersonaOverrides.toJSON(message.overrides);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateRequest>, I>>(base?: I): GenerateRequest {
    return GenerateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateRequest>, I>>(object: I): GenerateRequest {
    const message = createBaseGenerateRequest();
    message.personaId = object.personaId ?? "";
    message.overrides = (object.overrides !== undefined && object.overrides !== null)
      ? PersonaOverrides.fromPartial(object.overrides)
      : undefined;
    return message;
  },
};

function createBasePersonaOverrides(): PersonaOverrides {
  return { hairStyle: "", eyeColor: "", outfit: "" };
}

export const PersonaOverrides: MessageFns<PersonaOverrides> = {
  encode(message: PersonaOverrides, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hairStyle !== "") {
      writer.uint32(10).string(message.hairStyle);
    }
    if (message.eyeColor !== "") {
      writer.uint32(18).string(message.eyeColor);
    }
    if (message.outfit !== "") {
      writer.uint32(26).string(message.outfit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PersonaOverrides {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePersonaOverrides();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hairStyle = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.eyeColor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.outfit = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PersonaOverrides {
    return {
      hairStyle: isSet(object.hairStyle)
        ? globalThis.String(object.hairStyle)
        : isSet(object.hair_style)
        ? globalThis.String(object.hair_style)
        : "",
      eyeColor: isSet(object.eyeColor)
        ? globalThis.String(object.eyeColor)
        : isSet(object.eye_color)
        ? globalThis.String(object.eye_color)
        : "",
      outfit: isSet(object.outfit) ? globalThis.String(object.outfit) : "",
    };
  },

  toJSON(message: PersonaOverrides): unknown {
    const obj: any = {};
    if (message.hairStyle !== "") {
      obj.hairStyle = message.hairStyle;
    }
    if (message.eyeColor !== "") {
      obj.eyeColor = message.eyeColor;
    }
    if (message.outfit !== "") {
      obj.outfit = message.outfit;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PersonaOverrides>, I>>(base?: I): PersonaOverrides {
    return PersonaOverrides.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PersonaOverrides>, I>>(object: I): PersonaOverrides {
    const message = createBasePersonaOverrides();
    message.hairStyle = object.hairStyle ?? "";
    message.eyeColor = object.eyeColor ?? "";
    message.outfit = object.outfit ?? "";
    return message;
  },
};

function createBaseGenerateResponse(): GenerateResponse {
  return { imageUrl: "", metadata: {} };
}

export const GenerateResponse: MessageFns<GenerateResponse> = {
  encode(message: GenerateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageUrl !== "") {
      writer.uint32(10).string(message.imageUrl);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      GenerateResponse_MetadataEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imageUrl = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = GenerateResponse_MetadataEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.metadata[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateResponse {
    return {
      imageUrl: isSet(object.imageUrl)
        ? globalThis.String(object.imageUrl)
        : isSet(object.image_url)
        ? globalThis.String(object.image_url)
        : "",
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: GenerateResponse): unknown {
    const obj: any = {};
    if (message.imageUrl !== "") {
      obj.imageUrl = message.imageUrl;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateResponse>, I>>(base?: I): GenerateResponse {
    return GenerateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateResponse>, I>>(object: I): GenerateResponse {
    const message = createBaseGenerateResponse();
    message.imageUrl = object.imageUrl ?? "";
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseGenerateResponse_MetadataEntry(): GenerateResponse_MetadataEntry {
  return { key: "", value: "" };
}

export const GenerateResponse_MetadataEntry: MessageFns<GenerateResponse_MetadataEntry> = {
  encode(message: GenerateResponse_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateResponse_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateResponse_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateResponse_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: GenerateResponse_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateResponse_MetadataEntry>, I>>(base?: I): GenerateResponse_MetadataEntry {
    return GenerateResponse_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateResponse_MetadataEntry>, I>>(
    object: I,
  ): GenerateResponse_MetadataEntry {
    const message = createBaseGenerateResponse_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

/**
 * ImageGeneratorService defines how vtuber-brain or vtuber-api requests
 * images from vtuber-image.
 */
export type ImageGeneratorServiceDefinition = typeof ImageGeneratorServiceDefinition;
export const ImageGeneratorServiceDefinition = {
  name: "ImageGeneratorService",
  fullName: "vtuber_image.v1.ImageGeneratorService",
  methods: {
    generate: {
      name: "Generate",
      requestType: GenerateRequest as typeof GenerateRequest,
      requestStream: false,
      responseType: GenerateResponse as typeof GenerateResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
