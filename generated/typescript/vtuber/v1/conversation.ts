// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: vtuber/v1/conversation.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { PersonaId, personaIdFromJSON, personaIdToJSON } from "./persona";
import { ToolCall } from "./tool_call";
import {
  AudioFormat,
  audioFormatFromJSON,
  audioFormatToJSON,
  Emotion,
  emotionFromJSON,
  emotionToJSON,
} from "./voice_profile";

export const protobufPackage = "vtuber.v1";

/**
 * ConversationDirective is the single message vtuber-brain (the Director)
 * emits on every turn and vtuber-voice (the Performer) consumes. Per
 * ADR-002 this is the only protocol boundary between the two services.
 * Full audio streaming back to the Director is out of scope for v0.1.
 */
export interface ConversationDirective {
  /** Opaque per-turn id. Consumers MUST NOT parse it. UUIDv7 preferred. */
  directiveId: string;
  /** Emission time per the Director's clock. */
  emittedAt:
    | Date
    | undefined;
  /** The persona to wear for this turn. */
  personaId: PersonaId;
  /**
   * Text the Performer should speak. The Director owns all LLM
   * post-processing (sanitization, length trim) before emitting.
   */
  textPrompt: string;
  /**
   * Optional per-turn voice priming seed. When empty, the Performer
   * falls back to the persona's registered VoiceProfile in vtuber-commons.
   */
  voicePrompt: string;
  /** Per-turn emotion override. UNSPECIFIED means "use neutral". */
  emotion: Emotion;
  /** Requested output audio format. */
  targetAudioFormat: AudioFormat;
  /**
   * Soft upper bound on total synthesis latency in milliseconds.
   * 0 means unspecified — the Performer applies its default SLO.
   */
  maxLatencyMs: number;
  /**
   * Decentralized Tool Calls: These are intended for the Performer (Voice/Live2D)
   * to execute in sync with the speech (e.g., "animate", "play_sound_effect").
   */
  toolCalls: ToolCall[];
}

/**
 * DirectiveAck is the Performer's synchronous reply to EmitDirective.
 * A negative ack carries a stable short code; consumers branch on the
 * code, not on human-readable text.
 */
export interface DirectiveAck {
  directiveId: string;
  accepted: boolean;
  /**
   * Stable short code when accepted is false:
   * "persona_unknown", "format_unsupported", "queue_full",
   * "voice_profile_missing". Empty when accepted is true.
   */
  rejectReason: string;
}

/**
 * EmitDirectiveRequest wraps ConversationDirective for the rpc surface.
 * The wrapper exists because buf STANDARD lint requires rpc request/response
 * types to be named after the rpc — and because wrappers leave room for
 * per-rpc metadata (idempotency keys, request tracing) to be added without
 * touching the domain message.
 */
export interface EmitDirectiveRequest {
  directive: ConversationDirective | undefined;
}

/** EmitDirectiveResponse wraps DirectiveAck for the same reasons. */
export interface EmitDirectiveResponse {
  ack: DirectiveAck | undefined;
}

function createBaseConversationDirective(): ConversationDirective {
  return {
    directiveId: "",
    emittedAt: undefined,
    personaId: 0,
    textPrompt: "",
    voicePrompt: "",
    emotion: 0,
    targetAudioFormat: 0,
    maxLatencyMs: 0,
    toolCalls: [],
  };
}

export const ConversationDirective: MessageFns<ConversationDirective> = {
  encode(message: ConversationDirective, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.directiveId !== "") {
      writer.uint32(10).string(message.directiveId);
    }
    if (message.emittedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.emittedAt), writer.uint32(18).fork()).join();
    }
    if (message.personaId !== 0) {
      writer.uint32(24).int32(message.personaId);
    }
    if (message.textPrompt !== "") {
      writer.uint32(34).string(message.textPrompt);
    }
    if (message.voicePrompt !== "") {
      writer.uint32(42).string(message.voicePrompt);
    }
    if (message.emotion !== 0) {
      writer.uint32(48).int32(message.emotion);
    }
    if (message.targetAudioFormat !== 0) {
      writer.uint32(56).int32(message.targetAudioFormat);
    }
    if (message.maxLatencyMs !== 0) {
      writer.uint32(64).uint32(message.maxLatencyMs);
    }
    for (const v of message.toolCalls) {
      ToolCall.encode(v!, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConversationDirective {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConversationDirective();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.directiveId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.emittedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.personaId = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.textPrompt = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.voicePrompt = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.emotion = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.targetAudioFormat = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.maxLatencyMs = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.toolCalls.push(ToolCall.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConversationDirective {
    return {
      directiveId: isSet(object.directiveId)
        ? globalThis.String(object.directiveId)
        : isSet(object.directive_id)
        ? globalThis.String(object.directive_id)
        : "",
      emittedAt: isSet(object.emittedAt)
        ? fromJsonTimestamp(object.emittedAt)
        : isSet(object.emitted_at)
        ? fromJsonTimestamp(object.emitted_at)
        : undefined,
      personaId: isSet(object.personaId)
        ? personaIdFromJSON(object.personaId)
        : isSet(object.persona_id)
        ? personaIdFromJSON(object.persona_id)
        : 0,
      textPrompt: isSet(object.textPrompt)
        ? globalThis.String(object.textPrompt)
        : isSet(object.text_prompt)
        ? globalThis.String(object.text_prompt)
        : "",
      voicePrompt: isSet(object.voicePrompt)
        ? globalThis.String(object.voicePrompt)
        : isSet(object.voice_prompt)
        ? globalThis.String(object.voice_prompt)
        : "",
      emotion: isSet(object.emotion) ? emotionFromJSON(object.emotion) : 0,
      targetAudioFormat: isSet(object.targetAudioFormat)
        ? audioFormatFromJSON(object.targetAudioFormat)
        : isSet(object.target_audio_format)
        ? audioFormatFromJSON(object.target_audio_format)
        : 0,
      maxLatencyMs: isSet(object.maxLatencyMs)
        ? globalThis.Number(object.maxLatencyMs)
        : isSet(object.max_latency_ms)
        ? globalThis.Number(object.max_latency_ms)
        : 0,
      toolCalls: globalThis.Array.isArray(object?.toolCalls)
        ? object.toolCalls.map((e: any) => ToolCall.fromJSON(e))
        : globalThis.Array.isArray(object?.tool_calls)
        ? object.tool_calls.map((e: any) => ToolCall.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ConversationDirective): unknown {
    const obj: any = {};
    if (message.directiveId !== "") {
      obj.directiveId = message.directiveId;
    }
    if (message.emittedAt !== undefined) {
      obj.emittedAt = message.emittedAt.toISOString();
    }
    if (message.personaId !== 0) {
      obj.personaId = personaIdToJSON(message.personaId);
    }
    if (message.textPrompt !== "") {
      obj.textPrompt = message.textPrompt;
    }
    if (message.voicePrompt !== "") {
      obj.voicePrompt = message.voicePrompt;
    }
    if (message.emotion !== 0) {
      obj.emotion = emotionToJSON(message.emotion);
    }
    if (message.targetAudioFormat !== 0) {
      obj.targetAudioFormat = audioFormatToJSON(message.targetAudioFormat);
    }
    if (message.maxLatencyMs !== 0) {
      obj.maxLatencyMs = Math.round(message.maxLatencyMs);
    }
    if (message.toolCalls?.length) {
      obj.toolCalls = message.toolCalls.map((e) => ToolCall.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConversationDirective>, I>>(base?: I): ConversationDirective {
    return ConversationDirective.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConversationDirective>, I>>(object: I): ConversationDirective {
    const message = createBaseConversationDirective();
    message.directiveId = object.directiveId ?? "";
    message.emittedAt = object.emittedAt ?? undefined;
    message.personaId = object.personaId ?? 0;
    message.textPrompt = object.textPrompt ?? "";
    message.voicePrompt = object.voicePrompt ?? "";
    message.emotion = object.emotion ?? 0;
    message.targetAudioFormat = object.targetAudioFormat ?? 0;
    message.maxLatencyMs = object.maxLatencyMs ?? 0;
    message.toolCalls = object.toolCalls?.map((e) => ToolCall.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDirectiveAck(): DirectiveAck {
  return { directiveId: "", accepted: false, rejectReason: "" };
}

export const DirectiveAck: MessageFns<DirectiveAck> = {
  encode(message: DirectiveAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.directiveId !== "") {
      writer.uint32(10).string(message.directiveId);
    }
    if (message.accepted !== false) {
      writer.uint32(16).bool(message.accepted);
    }
    if (message.rejectReason !== "") {
      writer.uint32(26).string(message.rejectReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectiveAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDirectiveAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.directiveId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rejectReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DirectiveAck {
    return {
      directiveId: isSet(object.directiveId)
        ? globalThis.String(object.directiveId)
        : isSet(object.directive_id)
        ? globalThis.String(object.directive_id)
        : "",
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      rejectReason: isSet(object.rejectReason)
        ? globalThis.String(object.rejectReason)
        : isSet(object.reject_reason)
        ? globalThis.String(object.reject_reason)
        : "",
    };
  },

  toJSON(message: DirectiveAck): unknown {
    const obj: any = {};
    if (message.directiveId !== "") {
      obj.directiveId = message.directiveId;
    }
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.rejectReason !== "") {
      obj.rejectReason = message.rejectReason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectiveAck>, I>>(base?: I): DirectiveAck {
    return DirectiveAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectiveAck>, I>>(object: I): DirectiveAck {
    const message = createBaseDirectiveAck();
    message.directiveId = object.directiveId ?? "";
    message.accepted = object.accepted ?? false;
    message.rejectReason = object.rejectReason ?? "";
    return message;
  },
};

function createBaseEmitDirectiveRequest(): EmitDirectiveRequest {
  return { directive: undefined };
}

export const EmitDirectiveRequest: MessageFns<EmitDirectiveRequest> = {
  encode(message: EmitDirectiveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.directive !== undefined) {
      ConversationDirective.encode(message.directive, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmitDirectiveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmitDirectiveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.directive = ConversationDirective.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmitDirectiveRequest {
    return { directive: isSet(object.directive) ? ConversationDirective.fromJSON(object.directive) : undefined };
  },

  toJSON(message: EmitDirectiveRequest): unknown {
    const obj: any = {};
    if (message.directive !== undefined) {
      obj.directive = ConversationDirective.toJSON(message.directive);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EmitDirectiveRequest>, I>>(base?: I): EmitDirectiveRequest {
    return EmitDirectiveRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EmitDirectiveRequest>, I>>(object: I): EmitDirectiveRequest {
    const message = createBaseEmitDirectiveRequest();
    message.directive = (object.directive !== undefined && object.directive !== null)
      ? ConversationDirective.fromPartial(object.directive)
      : undefined;
    return message;
  },
};

function createBaseEmitDirectiveResponse(): EmitDirectiveResponse {
  return { ack: undefined };
}

export const EmitDirectiveResponse: MessageFns<EmitDirectiveResponse> = {
  encode(message: EmitDirectiveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ack !== undefined) {
      DirectiveAck.encode(message.ack, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmitDirectiveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmitDirectiveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ack = DirectiveAck.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmitDirectiveResponse {
    return { ack: isSet(object.ack) ? DirectiveAck.fromJSON(object.ack) : undefined };
  },

  toJSON(message: EmitDirectiveResponse): unknown {
    const obj: any = {};
    if (message.ack !== undefined) {
      obj.ack = DirectiveAck.toJSON(message.ack);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EmitDirectiveResponse>, I>>(base?: I): EmitDirectiveResponse {
    return EmitDirectiveResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EmitDirectiveResponse>, I>>(object: I): EmitDirectiveResponse {
    const message = createBaseEmitDirectiveResponse();
    message.ack = (object.ack !== undefined && object.ack !== null) ? DirectiveAck.fromPartial(object.ack) : undefined;
    return message;
  },
};

/**
 * DirectorService is the minimal service surface needed to validate tonic /
 * buf service codegen end-to-end for v0.1. The full audio streaming rpc
 * lives on a separate service added in the brain↔voice slice.
 */
export type DirectorServiceDefinition = typeof DirectorServiceDefinition;
export const DirectorServiceDefinition = {
  name: "DirectorService",
  fullName: "vtuber.v1.DirectorService",
  methods: {
    emitDirective: {
      name: "EmitDirective",
      requestType: EmitDirectiveRequest as typeof EmitDirectiveRequest,
      requestStream: false,
      responseType: EmitDirectiveResponse as typeof EmitDirectiveResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
